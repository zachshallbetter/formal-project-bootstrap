.DS_Store
__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
.coverage
.pytest_cache/
*.egg-info/
release/

# A global core.excludesFile commonly ignores these; only this file can override
# it, and without the negation `git add` silently stages nothing.
!AGENTS.md
!.agents/
!**/.agents/
